# PyTorch - Deep Learning Framework
torch>=1.9.0,<3.0.0
torchvision>=0.10.0,<1.0.0

# Computer Vision
opencv-python>=4.5.0,<5.0.0
mediapipe>=0.8.0,<1.0.0

# Scientific Computing
numpy>=1.21.0,<2.0.0

# Note: ROS 2 dependencies are managed via package.xml and rosdep
# Additional dependencies for MoveIt 2 should be installed via apt:
# sudo apt install ros-humble-moveit ros-humble-moveit-commander
